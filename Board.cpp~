#include<fstream>
#include<iostream>
using namespace std;
#include "Board.h"


Board::Board(const char* filename)
{
  ifstream g(filename);
  board = new Space *[40];
  for(int i = 0; i < 40; i++)
    {
      board[i]= new Space(g);
    }
  
}

